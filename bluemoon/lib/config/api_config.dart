class ApiConfig {
  static const String baseUrl = 'http://localhost:3001/api';
  // To use with Android emulator, if backend is on host machine: 'http://10.0.2.2:3001/api'
  // To use with iOS simulator, if backend is on host machine: 'http://localhost:3001/api' or host's actual IP.
} 