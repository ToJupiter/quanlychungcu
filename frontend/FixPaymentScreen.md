Our tasks:
- Fix the payment screen with the errors indicated in the terminal error included in the code.
- Redesign the Apartment Management screen and Household Management screen tables to use a modern, interactive Material UI style. The new table should be centered on the page, with proportional column widths where larger content (like descriptions or addresses) occupies more space. Implement visual enhancements such as alternating row colors, hover effects, and embedded sorting/filtering controls in headers. Include action buttons per row (e.g., Edit, Delete, View) styled with Material UI icons. Ensure the table has subtle elevation and padding consistent with Material Design principles. On mobile, optimize the layout by stacking columns vertically or converting rows into cards with expandable details. Use animations for sorting, filtering, and pagination transitions to enhance user experience

- With the table Design just like the ones above, generate me another screens and add to the Navigation Bar to manage the employees in charge of the building management. This will be linked to the corresponding table and the backend. Please read the bluemoon_database.txt to understand the schema and read the backend code carefully for API grasping. Also, make sure that the data types of the Flutter frontend is compatible with those backend and database. If it is not compatible, consider forcing the datatypes passing to the backend in its compatible type. Do not pass incompatible datatypes and remember we are using MySQL, so find me the data types compatible with it.

- We also need to enhance the buttons by giving them a logo. Some buttons are blank and without logo, some are duplicated (they lead to the same screen). These button errors happen in all screens. And also, there is a logic bug in the revenue calculation of the Dashboard code. It does not take into account the updated "Paid" revenue in the Payment screen. 


- Some buttons show its real logo when the layout is vertical and narrow. But when 